[project]
name = "osah"
version = "0.8.3"
description = "Local occupational safety desktop system."
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.1.2",
]

[tool.unittest]
start-directory = "tests"
